{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:base", ":disableDependencyDashboard"],
  "packageRules": [
    {
      "matchPackagePatterns": ["^gittools"],
      "allowedVersions": "<4"
    },
    {
      "matchPackagePatterns": ["Test", "Assert", "Mock", "[Xx]unit", "Moq"],
      "groupName": "Testing deps",
      "automerge": true,
      "automergeType": "branch"
    },
    {
      "matchPackagePatterns": ["docfx", "SourceLink", "CodeAnalysis"],
      "automerge": true,
      "automergeType": "branch"
    },
    {
      "description": "This library ships inside source generator packages, so it must reference the oldest Roslyn it needs; hosts running an older compiler (e.g. docfx) refuse to load analyzer assemblies that reference a newer one",
      "matchPackageNames": ["Microsoft.CodeAnalysis.CSharp"],
      "matchFileNames": ["src/CodeGeneration/CodeGeneration.csproj"],
      "allowedVersions": "<4.9"
    }
  ]
}
